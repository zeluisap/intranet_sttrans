<div class="conteudo_controller">
    <div class="row-fluid">
        <div class="span12">
            <form action="<?php echo $this->url(array("controller" => $this->getRequest()->getControllerName(), "action" => "editar")); ?>" method="post" id="formulario" class="formulario form-horizontal">
                <input type="hidden" name="id" id="id" value="<?php echo $this->registro->getId(); ?>" />
                <div class="well">
                    <fieldset>
                        <legend>CADASTRO DE GRUPO DE TRANSPORTE: </legend>
                <?php if ($this->registro->getId()) { ?>
                        <dl class="dl-horizontal">
                            <dt>Cï¿½digo:</dt>
                            <dd><?php echo $this->registro->getId(); ?></dd>
                        </dl>
                <?php } ?>
                        <div class="control-group">
                            <label for="chave" class="control-label">Chave:</label>
                            <div class="controls">
                                <input type="text" name="chave" id="chave" size="5" value="<?php echo $this->registro->chave; ?>" class="span2" />
                            </div>
                        </div>
                        <div class="control-group">
                            <label for="descricao" class="control-label">Descriï¿½ï¿½o:</label>
                            <div class="controls">
                                <input type="text" name="descricao" id="descricao" size="60" maxlength="60" value="<?php echo $this->registro->descricao; ?>" class="span5" />
                            </div>
                        </div>
                        <div class="control-group">
                            <label for="possui_concessao" class="control-label">Possui Concessï¿½o:</label>
                            <div class="controls">
                                <select name="possui_concessao" id="possui_concessao">
                                    <option value="S" <?php echo ($this->registro->possui_concessao())?"selected":""; ?>>SIM</option>
                                    <option value="N" <?php echo (!$this->registro->possui_concessao())?"selected":""; ?>>Nï¿½O</option>
                                </select>
                            </div>
                        </div>
                        <div class="control-group">
                            <label for="veiculo_unico" class="control-label">VeÃ­culo Ãnico:</label>
                            <div class="controls">
                                <select name="veiculo_unico" id="veiculo_unico">
                                    <option value="S" <?php echo ($this->registro->veiculo_unico)?"selected":""; ?>>SIM</option>
                                    <option value="N" <?php echo (!$this->registro->veiculo_unico)?"selected":""; ?>>Nï¿½O</option>
                                </select>
                            </div>
                        </div>
<?php
$ctrl = new Escola_Form_Element_Select_Table("id_banco_convenio");
$ctrl->setPkName("id_banco_convenio");
$ctrl->setModel("TbBancoConvenio");
$ctrl->setValue($this->registro->id_banco_convenio);
$ctrl->setLabel("Convï¿½nio: ");
echo $ctrl->render($this);
?>
                    </fieldset>
                </div>
            </form>
        </div>
    </div>
</div>